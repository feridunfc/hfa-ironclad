
# Best-of-Both Final Closure Patch

This pack merges the strongest parts of both closure packs:

- richer runtime-resolved auth model
- persistent hysteresis with compatibility-friendly API
- strict production/staging Lua CAS
- single authoritative worker picker
- stronger docs + acceptance tests
- restore/requeue consistency guard

Use as a repo-adapted merge pack, not blind overwrite.

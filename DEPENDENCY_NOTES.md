
# Dependency Notes

Collection errors also showed missing third-party deps in the environment:

Required for many existing tests:
```bash
pip install fakeredis openai
```

Bunlar bu zip içinde kod olarak eklenmez; ortam bağımlılığıdır.

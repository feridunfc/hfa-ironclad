[tool.ruff]
line-length = 100

[tool.ruff.lint]
ignore = [
    "F841",  # unused variable
]

exclude = [
    "archive_disabled",
    "tests/legacy",
]

[tool.ruff.per-file-ignores]
"hfa-tools/**" = ["F401", "F821", "F841", "E731"]